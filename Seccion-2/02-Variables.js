// Comentarios: Son lineas de código que el interprete de JavaScript ignorará a la hora de ejecutarlo

// Variable: Es un contenedor de información que apunta a un lugar de memoria
// Dicha información puede cambiar en el futuro

// Tres formas de crear una variable
let a = 10;

// Forma antigua de crear variables
var b = 10;

const c = 10; //Este valor no se va a poder cambiar

// Polyfill: Es un código que provee el funcionamiento de una nueva
// característica de JavaScript (ES6), en versiones viejas como ES5

let d = 10,
    e = 20,
    f = 10,
    g = 10,
    x = d + e;

console.log(x);

// El principal problema del Var es que permite reemplazar propiedades y objetos propios del objeto global




